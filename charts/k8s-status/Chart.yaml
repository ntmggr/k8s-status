apiVersion: v2
name: k8s-status
description: Read-only per-cluster status page for ArgoCD Application health.
type: application
# Stamped by .github/workflows/tag.yml onto the tagged commit, so a released
# chart always matches its image. Reads 0.0.0-dev on main; that is expected.
version: 0.20.1
appVersion: "0.20.1"
kubeVersion: ">=1.24.0-0"
keywords:
  - argocd
  - status
  - dashboard
  - read-only
  - platform
