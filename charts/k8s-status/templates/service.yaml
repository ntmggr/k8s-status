apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-status.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8s-status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "k8s-status.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
