apiVersion: v1
kind: Service
metadata:
  name: {{ include "srv-status.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "srv-status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "srv-status.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
